<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <title>To Do List v1</title>
  <link rel="stylesheet" href="css/styles.css">
</head>
<body>
  <div class="box" id="heading">
      <h1> <%= listTitle %> </h1>
      <!-- ur variable name -->
  </div>
  <div class="box">
    <!-- for my css -->


    <% newListItems.forEach(function(item){%>
  <!-- it loops through our array for any number of items there -->
  <!-- ie array of newListItems and tap singular item inside the arraytrigerring call back  -->

  <!-- form to delete items  -->
      <form class="deleteButton" action="/delete" method="post">

        <!-- our form makes a post request to the home route -->
        <div class="item">
          <input type="checkbox" name="checkbox" value=<%=item._id %> onChange="this.form.submit()">

          <!-- onChange code deletes the checkbox whwn checked tapping into the id  -->
            <p><%= item.name %></p>
            <!-- we render item.name to tap only the names in our items -->
        </div>
          <% }) %>
          <input type="hidden" name="listName" value="<%= listTitle %>"></input>
          <!-- to delete fromour customlist and not to send it to home route -->
      </form>

  <form class="item" action="/" method="post">
    <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
    <!-- it post a type of text  with name of new item -->
    <button type="submit" name="list" value=<%= listTitle%>>+</button>
  </form>
</div>
</body>
<!-- auto complete off is to avoid suggestions -->
</html>
